
<h1><center>Have a cup of <br />coffee</center></h1>
<%= image_tag "katz_over.jpg", :size => "400x300" %>

<table>
  <tr>
    <th>Thoughts</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<br />
<% @posts.each do |post| %>
  <br />
  <tr>
    <td><%= post.title %></td>
    <td>- _ -<%= post.name %>- _ -</td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
  <br />
<% end %>
</table>

<%= link_to 'New Post', new_post_path %>

